vocabulary {
    p q
    important(`Symbols)
}
theory {
  { p.}
  ~p()|q.
  { important(`p). }
  ?`x[`Symbols]: important(`x).
  ~(!`x[`Symbols]: important(`x)).

  // TODO 
  // !`x[`Symbols]: important(`x) => `x().
}
display {
  relevant(`p).
  expanded(`q).
  goal(`p).
  view = normal.
}