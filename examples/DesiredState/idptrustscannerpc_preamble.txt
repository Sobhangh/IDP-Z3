include <table_utils>

procedure main() {

    stdoptions.xsb = false
    S = calculatedefinitions(T,S)
    out = outputSymbols()

    json = require "dkjson"
    print(json.encode(encodeStruc(S,out)))
}
