vocabulary {
	type node := {a,b,c}
	edge: (node ⨯ node) → Bool
	symmetric: (Symbol) → Bool
    Start: ()→node
}
theory {
    edge(a,b).
    !`p in Symbol: symmetric(`p) & arity(`p)=2 & input_domain(`p,1)=input_domain(`p,1)
				=> (!x: !y in $(input_domain(`p,2)): $(`p)(x,y) => $(`p)(y,x)).
	symmetric := {`edge}
}
display {
	expand(`edge).
}