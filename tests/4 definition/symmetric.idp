vocabulary {
	type node constructed from {a,b,c}
	edge(node, node)
	symmetric(`Symbols)
}
theory {
    edge(a,b).
    !`p[`Symbols]: symmetric(`p) => (!x y : `p(x,y) => `p(y,x)).
	symmetric = {`edge}
}
display {
	expand(`edge).
}