vocabulary {
	type node constructed from {a,b,c}
	edge(node, node)
	symmetric(`Symbols)
}
theory {
    edge(a,b).
	symmetric(`edge). 
    { !`p[`Symbols]: symmetric(`p) <- (!x y : `p(x,y) => `p(y,x)).}
}
display {
	expand(`edge).
}