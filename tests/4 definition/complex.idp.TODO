vocabulary {
    type num = {-1; 0; 1}
    same(num) : num
    negated(num) : num
} 

theory {
    { !x[num]: same(x) = x. }
    { !x[num]: same(x) = -(-x). }
    { !x[num]: negated(same(x)) = (0-x). }
    same(1)= 1.
    same(same(-1))= negated(negated(-1)).

}
 
view expanded