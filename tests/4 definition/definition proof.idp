vocabulary {
    type color constructed from {red, blue}
    type code constructed from {a,b}
    codeOf: (color) → code
    c: ()→color
}

theory {
    { !cl: ! cd[code]: codeOf(cl)=cd <- (cl=c & cd=a) | (cl~=c & cd=b). }
    codeOf(red)=b.
}

display {
    view = expanded.
}
