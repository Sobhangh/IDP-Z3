vocabulary {
    p q r
    type color constructed from {red, blue, green}
    Color:color
}
theory {
    if p then q else r.
    Color() = (if p then blue else green).
}

procedure main() {
    print(decision_table(T, first_hit=False))
}