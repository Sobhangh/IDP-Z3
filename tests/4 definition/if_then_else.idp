vocabulary {
    p, q, r: () → Bool
    type color := {red, blue, green}
    Color: () →color
}
theory {
    if p() then q() else r().
    Color() = if p() then blue else green.
}

procedure main() {
    pretty_print(decision_table(T, first_hit=False))
}