vocabulary {
	type node := {a,b,c}
	edge: (node ⨯ node) → Bool
	symmetric: (Symbol) → Bool
}
theory {
    edge(a,b).
    { !`p in Symbol: symmetric(`p) <- !x, y : `p(x,y) => `p(y,x). }
	symmetric(`edge).
}
display {
	expand(`edge).
}