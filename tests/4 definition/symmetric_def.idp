vocabulary {
	type node := {a,b,c}
	edge: (node ⨯ node) → Bool
	symmetric: (`Symbols) → Bool
}
theory {
    edge(a,b).
    { !`p in `Symbols: symmetric(`p) <- !x, y : `p(x,y) => `p(y,x). }
	symmetric(`edge).
}
display {
	expand(`edge).
}