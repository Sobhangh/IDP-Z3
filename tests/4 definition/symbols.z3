And(Or(Not(p), q),
    important(`p),
    True,
    True,
    True,
    True,
    True,
    True,
    True,
    f(`f) == `f,
    Or(opposite(red) == red,
       opposite(red) == blue,
       opposite(red) == green),
    Or(opposite(blue) == red,
       opposite(blue) == blue,
       opposite(blue) == green),
    Or(opposite(green) == red,
       opposite(green) == blue,
       opposite(green) == green),
    Or(f(`𝔹) == `𝔹,
       f(`𝔹) == `ℤ,
       f(`𝔹) == `ℝ,
       f(`𝔹) == `Date,
       f(`𝔹) == `Concept,
       f(`𝔹) == `color,
       f(`𝔹) == `p,
       f(`𝔹) == `q,
       f(`𝔹) == `important,
       f(`𝔹) == `opposite,
       f(`𝔹) == `f),
    Or(f(`ℤ) == `𝔹,
       f(`ℤ) == `ℤ,
       f(`ℤ) == `ℝ,
       f(`ℤ) == `Date,
       f(`ℤ) == `Concept,
       f(`ℤ) == `color,
       f(`ℤ) == `p,
       f(`ℤ) == `q,
       f(`ℤ) == `important,
       f(`ℤ) == `opposite,
       f(`ℤ) == `f),
    Or(f(`ℝ) == `𝔹,
       f(`ℝ) == `ℤ,
       f(`ℝ) == `ℝ,
       f(`ℝ) == `Date,
       f(`ℝ) == `Concept,
       f(`ℝ) == `color,
       f(`ℝ) == `p,
       f(`ℝ) == `q,
       f(`ℝ) == `important,
       f(`ℝ) == `opposite,
       f(`ℝ) == `f),
    Or(f(`Date) == `𝔹,
       f(`Date) == `ℤ,
       f(`Date) == `ℝ,
       f(`Date) == `Date,
       f(`Date) == `Concept,
       f(`Date) == `color,
       f(`Date) == `p,
       f(`Date) == `q,
       f(`Date) == `important,
       f(`Date) == `opposite,
       f(`Date) == `f),
    Or(f(`Concept) == `𝔹,
       f(`Concept) == `ℤ,
       f(`Concept) == `ℝ,
       f(`Concept) == `Date,
       f(`Concept) == `Concept,
       f(`Concept) == `color,
       f(`Concept) == `p,
       f(`Concept) == `q,
       f(`Concept) == `important,
       f(`Concept) == `opposite,
       f(`Concept) == `f),
    Or(f(`color) == `𝔹,
       f(`color) == `ℤ,
       f(`color) == `ℝ,
       f(`color) == `Date,
       f(`color) == `Concept,
       f(`color) == `color,
       f(`color) == `p,
       f(`color) == `q,
       f(`color) == `important,
       f(`color) == `opposite,
       f(`color) == `f),
    Or(f(`p) == `𝔹,
       f(`p) == `ℤ,
       f(`p) == `ℝ,
       f(`p) == `Date,
       f(`p) == `Concept,
       f(`p) == `color,
       f(`p) == `p,
       f(`p) == `q,
       f(`p) == `important,
       f(`p) == `opposite,
       f(`p) == `f),
    Or(f(`q) == `𝔹,
       f(`q) == `ℤ,
       f(`q) == `ℝ,
       f(`q) == `Date,
       f(`q) == `Concept,
       f(`q) == `color,
       f(`q) == `p,
       f(`q) == `q,
       f(`q) == `important,
       f(`q) == `opposite,
       f(`q) == `f),
    Or(f(`important) == `𝔹,
       f(`important) == `ℤ,
       f(`important) == `ℝ,
       f(`important) == `Date,
       f(`important) == `Concept,
       f(`important) == `color,
       f(`important) == `p,
       f(`important) == `q,
       f(`important) == `important,
       f(`important) == `opposite,
       f(`important) == `f),
    Or(f(`opposite) == `𝔹,
       f(`opposite) == `ℤ,
       f(`opposite) == `ℝ,
       f(`opposite) == `Date,
       f(`opposite) == `Concept,
       f(`opposite) == `color,
       f(`opposite) == `p,
       f(`opposite) == `q,
       f(`opposite) == `important,
       f(`opposite) == `opposite,
       f(`opposite) == `f),
    Or(f(`f) == `𝔹,
       f(`f) == `ℤ,
       f(`f) == `ℝ,
       f(`f) == `Date,
       f(`f) == `Concept,
       f(`f) == `color,
       f(`f) == `p,
       f(`f) == `q,
       f(`f) == `important,
       f(`f) == `opposite,
       f(`f) == `f),
    important(`p) == p,
    And(Not(important(`𝔹)),
        Not(important(`ℤ)),
        Not(important(`ℝ)),
        Not(important(`Date)),
        Not(important(`Concept)),
        Not(important(`color)),
        important(`p) == p,
        Not(important(`q)),
        Not(important(`important)),
        Not(important(`opposite)),
        Not(important(`f))))
