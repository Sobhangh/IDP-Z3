vocabulary {
    type color := {red, blue}
    type code := {a,b}
    codeOf(color): code
}

theory {
    { !cl[color]: ! cd[code]: codeOf(cl)=cd <- (cl=red & cd=a) | (cl=blue & cd=b). }
}

display {
    view = expanded.
}
