vocabulary {
    p, q: () → Bool
    important: (`Symbols) → Bool
}
theory {
  ~p()|q().
  { important(`p). }

  { !`x in `Symbols: important(`x) <- `x(). }
}
display {
  relevant(`p).
  expand(`q).
  hide(`important).
  goal(`p).
  view() = normal.
}