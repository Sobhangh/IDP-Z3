vocabulary {
    p, q: ()
    important: (`Symbols)
}
theory {
  ~p()|q.
  { important(`p). }

  { !`x[`Symbols]: important(`x) <- `x(). }
}
display {
  relevant(`p).
  expand(`q).
  hide(`important).
  goal(`p).
  view = normal.
}