vocabulary {
    p, q: () → Bool
    important: (Symbol) → Bool
}
theory {
  ~p()|q().
  { important(`p). }

  { !`x in Symbol: important(`x) <- `x(). }
}
display {
  relevant(`p).
  expand(`q).
  hide(`important).
  goal(`p).
  view() = normal.
}