vocabulary {
    Works(ℝ)
    type temp = {0; 1; 2}
    p q
}
theory {
   p <=> (!t[temp]: 1 < t < 3 => 0 < t < 3).
   q <=> (!t[ℝ]: 1 < t < 3 => 0 < t < 3).
   ?x[Real]: Works(x).
}
display {
    view = expanded.
}