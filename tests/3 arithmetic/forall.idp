vocabulary {
    Works: (ℝ) → Bool
    type temp := {0, 1, 2}
    p, q:() → Bool
}
theory {
   p() <=> (!t ∈ temp: 1 < t < 3 => 0 < t < 3).
   q() <=> (!tintin in ℝ: 1 < tintin < 3 => 0 < tintin < 3).
   ?x in Real: Works(x).
}
display {
    view() = expanded.
}