vocabulary {
    double: (Real)→ Real
    quad: Real -> Real
    X: ()→Real
    y: ()→Real
}
theory {
    {!x[Real]: double(x)= x+x. }
    {!x[Real]: quad(x)= double(double(x)). }
    X()=3.
    y()=quad(X()).
}