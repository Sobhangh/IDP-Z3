vocabulary V {
    p, q, r:() → Bool
}

theory T:V {
   r()=>p(). q().
}

procedure main() {
	print(decision_table(T, goal_string="p"))
	print(decision_table(T, goal_string="p", verify=True))
}
//TODO q should not appear