vocabulary V {
    p, q, r:() → Bool
}

theory T:V {
   p()=>q().
   q()=>r().
}

procedure main() {
	print(decision_table(T, goal_string="q"))
	print(decision_table(T, goal_string="q", verify=True))
}
//TODO should distinguish between forward and backward reasoning