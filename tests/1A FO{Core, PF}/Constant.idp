vocabulary V {
    has_c : () -> Bool
    c : () -> Int (domain: has_c)
}

theory T:V {
}
structure S:V {
    c := 1.
}
procedure main() {
    Th = Theory(T, S)
    print("Formula: ")
    pretty_print(Th.formula())
    print("WDF: ")
    pretty_print(Th.WDFs)
    pretty_print(model_expand(T, S))
}