vocabulary V {
    has_c : () -> Bool
    c : has_c -> Int
}

theory T:V {
}
structure S:V {
    c := 1.
}
procedure main() {
    Th = Theory(T, S)
    print("Formula: ")
    pretty_print(Th.formula())
    print("WDF: ")
    pretty_print(Th.WDFs)
    pretty_print(model_expand(T, S))
}