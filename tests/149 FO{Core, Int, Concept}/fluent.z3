
Model 1
==========
on := {}.
put := {}.
remove := {0, 2}.
fluent := {(`on,false,`on,`on), (`on,false,`on,`put), (`on,false,`on,`remove), (`on,false,`put,`on), (`on,false,`put,`put), (`on,false,`put,`remove)}.


More models may be available.  Change the max argument to see them.
