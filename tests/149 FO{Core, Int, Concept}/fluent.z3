
Model 1
==========
on := {2, 3}.
put := {1, 2}.
remove := {1, 2}.
fluent := {(`on, false, `put, `put), (`on, false, `put, `remove), (`on, false, `remove, `put), (`on, false, `remove, `remove)}.


More models may be available.  Change the max argument to see them.
