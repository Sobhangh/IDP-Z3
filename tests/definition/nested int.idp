vocabulary {
    type num = {-1; 0; 1}
    same(num) : num
    negated(num) : num
} 

theory {
    { !x[num]: same(x) = x. }
    { !x[num]: negated(x) = (0-x). }
    same(-1)= -1.
    negated(negated(-1)) = same(-1).

}
 
view expanded
