vocabulary {
    type num = {-1; 0; 1}
    same(num) : num
    //negated(num) : num
} 

theory {
    { !x[num]: !y[num]: same(x) = y <- y=x. } //TODO { !x[num]: same(x) = x. }
    //{ !x[num]: !y[num]: negated(x) = y <- y = (0-x). } //TODO { !x[num]: negated(x) = (0-x). }
    same(-1)= -1.

}
 
view expanded

NOt satisfiable !!!!