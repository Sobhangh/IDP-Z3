
|------------
|(codeOf($codeOf!0$) = $codeOf$) ⇔ ((($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b))) with codeOf($codeOf!0$) = $codeOf$,((($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b)))
|+ $codeOf!0$ -> red
    |------------
    |codeOf($codeOf!0$) = $codeOf$ with codeOf($codeOf!0$),$codeOf$
    |+ $codeOf!0$ -> red
        |------------
        |codeOf($codeOf!0$) with $codeOf!0$
        |+ $codeOf!0$ -> red
            |------------
            |$codeOf!0$
            |+ $codeOf!0$ -> red
            |== red
        |== codeOf(red) with red
        |------------
        |$codeOf$
        |+ $codeOf!0$ -> red
        |== $codeOf$
    |== codeOf(red) = $codeOf$ with codeOf(red),$codeOf$
    |------------
    |((($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b))) with (($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b))
    |+ $codeOf!0$ -> red
        |------------
        |(($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b)) with (($codeOf!0$ = c) ∧ ($codeOf$ = a)),((~($codeOf!0$ = c)) ∧ ($codeOf$ = b))
        |+ $codeOf!0$ -> red
            |------------
            |(($codeOf!0$ = c) ∧ ($codeOf$ = a)) with ($codeOf!0$ = c) ∧ ($codeOf$ = a)
            |+ $codeOf!0$ -> red
                |------------
                |($codeOf!0$ = c) ∧ ($codeOf$ = a) with $codeOf!0$ = c,$codeOf$ = a
                |+ $codeOf!0$ -> red
                    |------------
                    |$codeOf!0$ = c with $codeOf!0$,c
                    |+ $codeOf!0$ -> red
                        |------------
                        |$codeOf!0$
                        |+ $codeOf!0$ -> red
                        |== red
                        |------------
                        |c
                        |+ $codeOf!0$ -> red
                        |== c
                    |== red = c with red,c
                    |------------
                    |$codeOf$ = a with $codeOf$,a
                    |+ $codeOf!0$ -> red
                        |------------
                        |$codeOf$
                        |+ $codeOf!0$ -> red
                        |== $codeOf$
                        |------------
                        |a
                        |+ $codeOf!0$ -> red
                        |== a
                    |== $codeOf$ = a with $codeOf$,a
                |== (red = c) ∧ ($codeOf$ = a) with red = c,$codeOf$ = a
            |== ((red = c) ∧ ($codeOf$ = a)) with (red = c) ∧ ($codeOf$ = a)
            |------------
            |((~($codeOf!0$ = c)) ∧ ($codeOf$ = b)) with (~($codeOf!0$ = c)) ∧ ($codeOf$ = b)
            |+ $codeOf!0$ -> red
                |------------
                |(~($codeOf!0$ = c)) ∧ ($codeOf$ = b) with ~($codeOf!0$ = c),$codeOf$ = b
                |+ $codeOf!0$ -> red
                    |------------
                    |~($codeOf!0$ = c) with $codeOf!0$ = c
                    |+ $codeOf!0$ -> red
                        |------------
                        |$codeOf!0$ = c with $codeOf!0$,c
                        |+ $codeOf!0$ -> red
                            |------------
                            |$codeOf!0$
                            |+ $codeOf!0$ -> red
                            |== red
                            |------------
                            |c
                            |+ $codeOf!0$ -> red
                            |== c
                        |== red = c with red,c
                    |== ~(red = c) with red = c
                    |------------
                    |$codeOf$ = b with $codeOf$,b
                    |+ $codeOf!0$ -> red
                        |------------
                        |$codeOf$
                        |+ $codeOf!0$ -> red
                        |== $codeOf$
                        |------------
                        |b
                        |+ $codeOf!0$ -> red
                        |== b
                    |== $codeOf$ = b with $codeOf$,b
                |== (~(red = c)) ∧ ($codeOf$ = b) with ~(red = c),$codeOf$ = b
            |== ((~(red = c)) ∧ ($codeOf$ = b)) with (~(red = c)) ∧ ($codeOf$ = b)
        |== ((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b)) with ((red = c) ∧ ($codeOf$ = a)),((~(red = c)) ∧ ($codeOf$ = b))
    |== (((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b))) with ((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b))
|== (codeOf(red) = $codeOf$) ⇔ (((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b))) with codeOf(red) = $codeOf$,(((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b)))
|------------
|(codeOf($codeOf!0$) = $codeOf$) ⇔ ((($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b))) with codeOf($codeOf!0$) = $codeOf$,((($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b)))
|+ $codeOf!0$ -> blue
    |------------
    |codeOf($codeOf!0$) = $codeOf$ with codeOf($codeOf!0$),$codeOf$
    |+ $codeOf!0$ -> blue
        |------------
        |codeOf($codeOf!0$) with $codeOf!0$
        |+ $codeOf!0$ -> blue
            |------------
            |$codeOf!0$
            |+ $codeOf!0$ -> blue
            |== blue
        |== codeOf(blue) with blue
        |------------
        |$codeOf$
        |+ $codeOf!0$ -> blue
        |== $codeOf$
    |== codeOf(blue) = $codeOf$ with codeOf(blue),$codeOf$
    |------------
    |((($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b))) with (($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b))
    |+ $codeOf!0$ -> blue
        |------------
        |(($codeOf!0$ = c) ∧ ($codeOf$ = a)) ∨ ((~($codeOf!0$ = c)) ∧ ($codeOf$ = b)) with (($codeOf!0$ = c) ∧ ($codeOf$ = a)),((~($codeOf!0$ = c)) ∧ ($codeOf$ = b))
        |+ $codeOf!0$ -> blue
            |------------
            |(($codeOf!0$ = c) ∧ ($codeOf$ = a)) with ($codeOf!0$ = c) ∧ ($codeOf$ = a)
            |+ $codeOf!0$ -> blue
                |------------
                |($codeOf!0$ = c) ∧ ($codeOf$ = a) with $codeOf!0$ = c,$codeOf$ = a
                |+ $codeOf!0$ -> blue
                    |------------
                    |$codeOf!0$ = c with $codeOf!0$,c
                    |+ $codeOf!0$ -> blue
                        |------------
                        |$codeOf!0$
                        |+ $codeOf!0$ -> blue
                        |== blue
                        |------------
                        |c
                        |+ $codeOf!0$ -> blue
                        |== c
                    |== blue = c with blue,c
                    |------------
                    |$codeOf$ = a with $codeOf$,a
                    |+ $codeOf!0$ -> blue
                        |------------
                        |$codeOf$
                        |+ $codeOf!0$ -> blue
                        |== $codeOf$
                        |------------
                        |a
                        |+ $codeOf!0$ -> blue
                        |== a
                    |== $codeOf$ = a with $codeOf$,a
                |== (blue = c) ∧ ($codeOf$ = a) with blue = c,$codeOf$ = a
            |== ((blue = c) ∧ ($codeOf$ = a)) with (blue = c) ∧ ($codeOf$ = a)
            |------------
            |((~($codeOf!0$ = c)) ∧ ($codeOf$ = b)) with (~($codeOf!0$ = c)) ∧ ($codeOf$ = b)
            |+ $codeOf!0$ -> blue
                |------------
                |(~($codeOf!0$ = c)) ∧ ($codeOf$ = b) with ~($codeOf!0$ = c),$codeOf$ = b
                |+ $codeOf!0$ -> blue
                    |------------
                    |~($codeOf!0$ = c) with $codeOf!0$ = c
                    |+ $codeOf!0$ -> blue
                        |------------
                        |$codeOf!0$ = c with $codeOf!0$,c
                        |+ $codeOf!0$ -> blue
                            |------------
                            |$codeOf!0$
                            |+ $codeOf!0$ -> blue
                            |== blue
                            |------------
                            |c
                            |+ $codeOf!0$ -> blue
                            |== c
                        |== blue = c with blue,c
                    |== ~(blue = c) with blue = c
                    |------------
                    |$codeOf$ = b with $codeOf$,b
                    |+ $codeOf!0$ -> blue
                        |------------
                        |$codeOf$
                        |+ $codeOf!0$ -> blue
                        |== $codeOf$
                        |------------
                        |b
                        |+ $codeOf!0$ -> blue
                        |== b
                    |== $codeOf$ = b with $codeOf$,b
                |== (~(blue = c)) ∧ ($codeOf$ = b) with ~(blue = c),$codeOf$ = b
            |== ((~(blue = c)) ∧ ($codeOf$ = b)) with (~(blue = c)) ∧ ($codeOf$ = b)
        |== ((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b)) with ((blue = c) ∧ ($codeOf$ = a)),((~(blue = c)) ∧ ($codeOf$ = b))
    |== (((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b))) with ((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b))
|== (codeOf(blue) = $codeOf$) ⇔ (((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b))) with codeOf(blue) = $codeOf$,(((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b)))
|------------
|(codeOf(red) = $codeOf$) ⇔ (((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b))) with codeOf(red) = $codeOf$,(((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b)))
|+ $codeOf$ -> a
    |------------
    |codeOf(red) = $codeOf$ with codeOf(red),$codeOf$
    |+ $codeOf$ -> a
        |------------
        |codeOf(red) with red
        |+ $codeOf$ -> a
            |------------
            |red
            |+ $codeOf$ -> a
            |== red
        |== codeOf(red) with red
        |------------
        |$codeOf$
        |+ $codeOf$ -> a
        |== a
    |== codeOf(red) = a with codeOf(red),a
    |------------
    |(((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b))) with ((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b))
    |+ $codeOf$ -> a
        |------------
        |((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b)) with ((red = c) ∧ ($codeOf$ = a)),((~(red = c)) ∧ ($codeOf$ = b))
        |+ $codeOf$ -> a
            |------------
            |((red = c) ∧ ($codeOf$ = a)) with (red = c) ∧ ($codeOf$ = a)
            |+ $codeOf$ -> a
                |------------
                |(red = c) ∧ ($codeOf$ = a) with red = c,$codeOf$ = a
                |+ $codeOf$ -> a
                    |------------
                    |red = c with red,c
                    |+ $codeOf$ -> a
                        |------------
                        |red
                        |+ $codeOf$ -> a
                        |== red
                        |------------
                        |c
                        |+ $codeOf$ -> a
                        |== c
                    |== red = c with red,c
                    |------------
                    |$codeOf$ = a with $codeOf$,a
                    |+ $codeOf$ -> a
                        |------------
                        |$codeOf$
                        |+ $codeOf$ -> a
                        |== a
                        |------------
                        |a
                        |+ $codeOf$ -> a
                        |== a
                    |== !true with $codeOf$,a
                |== (red = c) with red = c
            |== ((red = c)) with (red = c)
            |------------
            |((~(red = c)) ∧ ($codeOf$ = b)) with (~(red = c)) ∧ ($codeOf$ = b)
            |+ $codeOf$ -> a
                |------------
                |(~(red = c)) ∧ ($codeOf$ = b) with ~(red = c),$codeOf$ = b
                |+ $codeOf$ -> a
                    |------------
                    |~(red = c) with red = c
                    |+ $codeOf$ -> a
                        |------------
                        |red = c with red,c
                        |+ $codeOf$ -> a
                            |------------
                            |red
                            |+ $codeOf$ -> a
                            |== red
                            |------------
                            |c
                            |+ $codeOf$ -> a
                            |== c
                        |== red = c with red,c
                    |== ~(red = c) with red = c
                    |------------
                    |$codeOf$ = b with $codeOf$,b
                    |+ $codeOf$ -> a
                        |------------
                        |$codeOf$
                        |+ $codeOf$ -> a
                        |== a
                        |------------
                        |b
                        |+ $codeOf$ -> a
                        |== b
                    |== !false with $codeOf$,b
                |== !false with $codeOf$ = b
            |== !false with ($codeOf$ = b)
        |== ((red = c)) with ((red = c))
    |== (((red = c))) with ((red = c))
|== (codeOf(red) = a) ⇔ (((red = c))) with codeOf(red) = a,(((red = c)))
|------------
|(codeOf(red) = $codeOf$) ⇔ (((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b))) with codeOf(red) = $codeOf$,(((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b)))
|+ $codeOf$ -> b
    |------------
    |codeOf(red) = $codeOf$ with codeOf(red),$codeOf$
    |+ $codeOf$ -> b
        |------------
        |codeOf(red) with red
        |+ $codeOf$ -> b
            |------------
            |red
            |+ $codeOf$ -> b
            |== red
        |== codeOf(red) with red
        |------------
        |$codeOf$
        |+ $codeOf$ -> b
        |== b
    |== codeOf(red) = b with codeOf(red),b
    |------------
    |(((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b))) with ((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b))
    |+ $codeOf$ -> b
        |------------
        |((red = c) ∧ ($codeOf$ = a)) ∨ ((~(red = c)) ∧ ($codeOf$ = b)) with ((red = c) ∧ ($codeOf$ = a)),((~(red = c)) ∧ ($codeOf$ = b))
        |+ $codeOf$ -> b
            |------------
            |((red = c) ∧ ($codeOf$ = a)) with (red = c) ∧ ($codeOf$ = a)
            |+ $codeOf$ -> b
                |------------
                |(red = c) ∧ ($codeOf$ = a) with red = c,$codeOf$ = a
                |+ $codeOf$ -> b
                    |------------
                    |red = c with red,c
                    |+ $codeOf$ -> b
                        |------------
                        |red
                        |+ $codeOf$ -> b
                        |== red
                        |------------
                        |c
                        |+ $codeOf$ -> b
                        |== c
                    |== red = c with red,c
                    |------------
                    |$codeOf$ = a with $codeOf$,a
                    |+ $codeOf$ -> b
                        |------------
                        |$codeOf$
                        |+ $codeOf$ -> b
                        |== b
                        |------------
                        |a
                        |+ $codeOf$ -> b
                        |== a
                    |== !false with $codeOf$,a
                |== !false with $codeOf$ = a
            |== !false with ($codeOf$ = a)
            |------------
            |((~(red = c)) ∧ ($codeOf$ = b)) with (~(red = c)) ∧ ($codeOf$ = b)
            |+ $codeOf$ -> b
                |------------
                |(~(red = c)) ∧ ($codeOf$ = b) with ~(red = c),$codeOf$ = b
                |+ $codeOf$ -> b
                    |------------
                    |~(red = c) with red = c
                    |+ $codeOf$ -> b
                        |------------
                        |red = c with red,c
                        |+ $codeOf$ -> b
                            |------------
                            |red
                            |+ $codeOf$ -> b
                            |== red
                            |------------
                            |c
                            |+ $codeOf$ -> b
                            |== c
                        |== red = c with red,c
                    |== ~(red = c) with red = c
                    |------------
                    |$codeOf$ = b with $codeOf$,b
                    |+ $codeOf$ -> b
                        |------------
                        |$codeOf$
                        |+ $codeOf$ -> b
                        |== b
                        |------------
                        |b
                        |+ $codeOf$ -> b
                        |== b
                    |== !true with $codeOf$,b
                |== (~(red = c)) with ~(red = c)
            |== ((~(red = c))) with (~(red = c))
        |== ((~(red = c))) with ((~(red = c)))
    |== (((~(red = c)))) with ((~(red = c)))
|== (codeOf(red) = b) ⇔ (((~(red = c)))) with codeOf(red) = b,(((~(red = c))))
|------------
|(codeOf(blue) = $codeOf$) ⇔ (((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b))) with codeOf(blue) = $codeOf$,(((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b)))
|+ $codeOf$ -> a
    |------------
    |codeOf(blue) = $codeOf$ with codeOf(blue),$codeOf$
    |+ $codeOf$ -> a
        |------------
        |codeOf(blue) with blue
        |+ $codeOf$ -> a
            |------------
            |blue
            |+ $codeOf$ -> a
            |== blue
        |== codeOf(blue) with blue
        |------------
        |$codeOf$
        |+ $codeOf$ -> a
        |== a
    |== codeOf(blue) = a with codeOf(blue),a
    |------------
    |(((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b))) with ((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b))
    |+ $codeOf$ -> a
        |------------
        |((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b)) with ((blue = c) ∧ ($codeOf$ = a)),((~(blue = c)) ∧ ($codeOf$ = b))
        |+ $codeOf$ -> a
            |------------
            |((blue = c) ∧ ($codeOf$ = a)) with (blue = c) ∧ ($codeOf$ = a)
            |+ $codeOf$ -> a
                |------------
                |(blue = c) ∧ ($codeOf$ = a) with blue = c,$codeOf$ = a
                |+ $codeOf$ -> a
                    |------------
                    |blue = c with blue,c
                    |+ $codeOf$ -> a
                        |------------
                        |blue
                        |+ $codeOf$ -> a
                        |== blue
                        |------------
                        |c
                        |+ $codeOf$ -> a
                        |== c
                    |== blue = c with blue,c
                    |------------
                    |$codeOf$ = a with $codeOf$,a
                    |+ $codeOf$ -> a
                        |------------
                        |$codeOf$
                        |+ $codeOf$ -> a
                        |== a
                        |------------
                        |a
                        |+ $codeOf$ -> a
                        |== a
                    |== !true with $codeOf$,a
                |== (blue = c) with blue = c
            |== ((blue = c)) with (blue = c)
            |------------
            |((~(blue = c)) ∧ ($codeOf$ = b)) with (~(blue = c)) ∧ ($codeOf$ = b)
            |+ $codeOf$ -> a
                |------------
                |(~(blue = c)) ∧ ($codeOf$ = b) with ~(blue = c),$codeOf$ = b
                |+ $codeOf$ -> a
                    |------------
                    |~(blue = c) with blue = c
                    |+ $codeOf$ -> a
                        |------------
                        |blue = c with blue,c
                        |+ $codeOf$ -> a
                            |------------
                            |blue
                            |+ $codeOf$ -> a
                            |== blue
                            |------------
                            |c
                            |+ $codeOf$ -> a
                            |== c
                        |== blue = c with blue,c
                    |== ~(blue = c) with blue = c
                    |------------
                    |$codeOf$ = b with $codeOf$,b
                    |+ $codeOf$ -> a
                        |------------
                        |$codeOf$
                        |+ $codeOf$ -> a
                        |== a
                        |------------
                        |b
                        |+ $codeOf$ -> a
                        |== b
                    |== !false with $codeOf$,b
                |== !false with $codeOf$ = b
            |== !false with ($codeOf$ = b)
        |== ((blue = c)) with ((blue = c))
    |== (((blue = c))) with ((blue = c))
|== (codeOf(blue) = a) ⇔ (((blue = c))) with codeOf(blue) = a,(((blue = c)))
|------------
|(codeOf(blue) = $codeOf$) ⇔ (((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b))) with codeOf(blue) = $codeOf$,(((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b)))
|+ $codeOf$ -> b
    |------------
    |codeOf(blue) = $codeOf$ with codeOf(blue),$codeOf$
    |+ $codeOf$ -> b
        |------------
        |codeOf(blue) with blue
        |+ $codeOf$ -> b
            |------------
            |blue
            |+ $codeOf$ -> b
            |== blue
        |== codeOf(blue) with blue
        |------------
        |$codeOf$
        |+ $codeOf$ -> b
        |== b
    |== codeOf(blue) = b with codeOf(blue),b
    |------------
    |(((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b))) with ((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b))
    |+ $codeOf$ -> b
        |------------
        |((blue = c) ∧ ($codeOf$ = a)) ∨ ((~(blue = c)) ∧ ($codeOf$ = b)) with ((blue = c) ∧ ($codeOf$ = a)),((~(blue = c)) ∧ ($codeOf$ = b))
        |+ $codeOf$ -> b
            |------------
            |((blue = c) ∧ ($codeOf$ = a)) with (blue = c) ∧ ($codeOf$ = a)
            |+ $codeOf$ -> b
                |------------
                |(blue = c) ∧ ($codeOf$ = a) with blue = c,$codeOf$ = a
                |+ $codeOf$ -> b
                    |------------
                    |blue = c with blue,c
                    |+ $codeOf$ -> b
                        |------------
                        |blue
                        |+ $codeOf$ -> b
                        |== blue
                        |------------
                        |c
                        |+ $codeOf$ -> b
                        |== c
                    |== blue = c with blue,c
                    |------------
                    |$codeOf$ = a with $codeOf$,a
                    |+ $codeOf$ -> b
                        |------------
                        |$codeOf$
                        |+ $codeOf$ -> b
                        |== b
                        |------------
                        |a
                        |+ $codeOf$ -> b
                        |== a
                    |== !false with $codeOf$,a
                |== !false with $codeOf$ = a
            |== !false with ($codeOf$ = a)
            |------------
            |((~(blue = c)) ∧ ($codeOf$ = b)) with (~(blue = c)) ∧ ($codeOf$ = b)
            |+ $codeOf$ -> b
                |------------
                |(~(blue = c)) ∧ ($codeOf$ = b) with ~(blue = c),$codeOf$ = b
                |+ $codeOf$ -> b
                    |------------
                    |~(blue = c) with blue = c
                    |+ $codeOf$ -> b
                        |------------
                        |blue = c with blue,c
                        |+ $codeOf$ -> b
                            |------------
                            |blue
                            |+ $codeOf$ -> b
                            |== blue
                            |------------
                            |c
                            |+ $codeOf$ -> b
                            |== c
                        |== blue = c with blue,c
                    |== ~(blue = c) with blue = c
                    |------------
                    |$codeOf$ = b with $codeOf$,b
                    |+ $codeOf$ -> b
                        |------------
                        |$codeOf$
                        |+ $codeOf$ -> b
                        |== b
                        |------------
                        |b
                        |+ $codeOf$ -> b
                        |== b
                    |== !true with $codeOf$,b
                |== (~(blue = c)) with ~(blue = c)
            |== ((~(blue = c))) with (~(blue = c))
        |== ((~(blue = c))) with ((~(blue = c)))
    |== (((~(blue = c)))) with ((~(blue = c)))
|== (codeOf(blue) = b) ⇔ (((~(blue = c)))) with codeOf(blue) = b,(((~(blue = c))))
|------------
|(codeOf(red) = a) ∨ (codeOf(red) = b) with codeOf(red) = a,codeOf(red) = b
|+ codeOf(red) -> b
    |------------
    |*codeOf(red) = a with codeOf(red),a
    |+ codeOf(red) -> b
        |------------
        |codeOf(red) with red
        |+ codeOf(red) -> b
        |== !b with red
        |------------
        |a
        |+ codeOf(red) -> b
        |== a
    |== *!false with codeOf(red),a
    |------------
    |*codeOf(red) = b with codeOf(red),b
    |+ codeOf(red) -> b
        |------------
        |codeOf(red) with red
        |+ codeOf(red) -> b
        |== !b with red
        |------------
        |b
        |+ codeOf(red) -> b
        |== b
    |== *!true with codeOf(red),b
|== !true with codeOf(red) = b
|------------
|(codeOf(blue) = a) ∨ (codeOf(blue) = b) with codeOf(blue) = a,codeOf(blue) = b
|+ codeOf(red) -> b
    |------------
    |*codeOf(blue) = a with codeOf(blue),a
    |+ codeOf(red) -> b
        |------------
        |codeOf(blue) with blue
        |+ codeOf(red) -> b
            |------------
            |blue
            |+ codeOf(red) -> b
            |== blue
        |== codeOf(blue) with blue
        |------------
        |a
        |+ codeOf(red) -> b
        |== a
    |== *codeOf(blue) = a with codeOf(blue),a
    |------------
    |*codeOf(blue) = b with codeOf(blue),b
    |+ codeOf(red) -> b
        |------------
        |codeOf(blue) with blue
        |+ codeOf(red) -> b
            |------------
            |blue
            |+ codeOf(red) -> b
            |== blue
        |== codeOf(blue) with blue
        |------------
        |b
        |+ codeOf(red) -> b
        |== b
    |== *codeOf(blue) = b with codeOf(blue),b
|== (codeOf(blue) = a) ∨ (codeOf(blue) = b) with codeOf(blue) = a,codeOf(blue) = b
|------------
|(c = red) ∨ (c = blue) with c = red,c = blue
|+ codeOf(red) -> b
    |------------
    |*c = red with c,red
    |+ codeOf(red) -> b
        |------------
        |c
        |+ codeOf(red) -> b
        |== c
        |------------
        |red
        |+ codeOf(red) -> b
        |== red
    |== *c = red with c,red
    |------------
    |*c = blue with c,blue
    |+ codeOf(red) -> b
        |------------
        |c
        |+ codeOf(red) -> b
        |== c
        |------------
        |blue
        |+ codeOf(red) -> b
        |== blue
    |== *c = blue with c,blue
|== (c = red) ∨ (c = blue) with c = red,c = blue
|------------
|*red = c with red,c
|+ codeOf(red) -> b
    |------------
    |red
    |+ codeOf(red) -> b
    |== red
    |------------
    |c
    |+ codeOf(red) -> b
    |== c
|== *red = c with red,c
|------------
|*codeOf(red) = a with codeOf(red),a
|+ codeOf(red) -> b
    |------------
    |codeOf(red) with red
    |+ codeOf(red) -> b
    |== !b with red
    |------------
    |a
    |+ codeOf(red) -> b
    |== a
|== *!false with codeOf(red),a
|------------
|*codeOf(blue) = a with codeOf(blue),a
|+ codeOf(red) -> b
    |------------
    |codeOf(blue) with blue
    |+ codeOf(red) -> b
        |------------
        |blue
        |+ codeOf(red) -> b
        |== blue
    |== codeOf(blue) with blue
    |------------
    |a
    |+ codeOf(red) -> b
    |== a
|== *codeOf(blue) = a with codeOf(blue),a
|------------
|*codeOf(blue) = b with codeOf(blue),b
|+ codeOf(red) -> b
    |------------
    |codeOf(blue) with blue
    |+ codeOf(red) -> b
        |------------
        |blue
        |+ codeOf(red) -> b
        |== blue
    |== codeOf(blue) with blue
    |------------
    |b
    |+ codeOf(red) -> b
    |== b
|== *codeOf(blue) = b with codeOf(blue),b
|------------
|*c = red with c,red
|+ codeOf(red) -> b
    |------------
    |c
    |+ codeOf(red) -> b
    |== c
    |------------
    |red
    |+ codeOf(red) -> b
    |== red
|== *c = red with c,red
|------------
|*c = blue with c,blue
|+ codeOf(red) -> b
    |------------
    |c
    |+ codeOf(red) -> b
    |== c
    |------------
    |blue
    |+ codeOf(red) -> b
    |== blue
|== *c = blue with c,blue
|------------
|(codeOf(blue) = a) ∨ (codeOf(blue) = b) with codeOf(blue) = a,codeOf(blue) = b
|+ codeOf(red) -> b
    |------------
    |*codeOf(blue) = a with codeOf(blue),a
    |+ codeOf(red) -> b
        |------------
        |codeOf(blue) with blue
        |+ codeOf(red) -> b
            |------------
            |blue
            |+ codeOf(red) -> b
            |== blue
        |== codeOf(blue) with blue
        |------------
        |a
        |+ codeOf(red) -> b
        |== a
    |== *codeOf(blue) = a with codeOf(blue),a
    |------------
    |*codeOf(blue) = b with codeOf(blue),b
    |+ codeOf(red) -> b
        |------------
        |codeOf(blue) with blue
        |+ codeOf(red) -> b
            |------------
            |blue
            |+ codeOf(red) -> b
            |== blue
        |== codeOf(blue) with blue
        |------------
        |b
        |+ codeOf(red) -> b
        |== b
    |== *codeOf(blue) = b with codeOf(blue),b
|== (codeOf(blue) = a) ∨ (codeOf(blue) = b) with codeOf(blue) = a,codeOf(blue) = b
|------------
|(c = red) ∨ (c = blue) with c = red,c = blue
|+ codeOf(red) -> b
    |------------
    |*c = red with c,red
    |+ codeOf(red) -> b
        |------------
        |c
        |+ codeOf(red) -> b
        |== c
        |------------
        |red
        |+ codeOf(red) -> b
        |== red
    |== *c = red with c,red
    |------------
    |*c = blue with c,blue
    |+ codeOf(red) -> b
        |------------
        |c
        |+ codeOf(red) -> b
        |== c
        |------------
        |blue
        |+ codeOf(red) -> b
        |== blue
    |== *c = blue with c,blue
|== (c = red) ∨ (c = blue) with c = red,c = blue
|------------
|*red = c with red,c
|+ codeOf(red) -> b
    |------------
    |red
    |+ codeOf(red) -> b
    |== red
    |------------
    |c
    |+ codeOf(red) -> b
    |== c
|== *red = c with red,c
|------------
|*codeOf(red) = a with codeOf(red),a
|+ codeOf(red) -> b
|== *!false with codeOf(red),a
|------------
|*codeOf(blue) = a with codeOf(blue),a
|+ codeOf(red) -> b
    |------------
    |codeOf(blue) with blue
    |+ codeOf(red) -> b
        |------------
        |blue
        |+ codeOf(red) -> b
        |== blue
    |== codeOf(blue) with blue
    |------------
    |a
    |+ codeOf(red) -> b
    |== a
|== *codeOf(blue) = a with codeOf(blue),a
|------------
|*codeOf(blue) = b with codeOf(blue),b
|+ codeOf(red) -> b
    |------------
    |codeOf(blue) with blue
    |+ codeOf(red) -> b
        |------------
        |blue
        |+ codeOf(red) -> b
        |== blue
    |== codeOf(blue) with blue
    |------------
    |b
    |+ codeOf(red) -> b
    |== b
|== *codeOf(blue) = b with codeOf(blue),b
|------------
|*c = red with c,red
|+ codeOf(red) -> b
    |------------
    |c
    |+ codeOf(red) -> b
    |== c
    |------------
    |red
    |+ codeOf(red) -> b
    |== red
|== *c = red with c,red
|------------
|*c = blue with c,blue
|+ codeOf(red) -> b
    |------------
    |c
    |+ codeOf(red) -> b
    |== c
    |------------
    |blue
    |+ codeOf(red) -> b
    |== blue
|== *c = blue with c,blue
Z3 propagation ********************************
|------------
|(codeOf(blue) = a) ∨ (codeOf(blue) = b) with codeOf(blue) = a,codeOf(blue) = b
|+ red = c -> false
    |------------
    |*codeOf(blue) = a with codeOf(blue),a
    |+ red = c -> false
        |------------
        |codeOf(blue) with blue
        |+ red = c -> false
            |------------
            |blue
            |+ red = c -> false
            |== blue
        |== codeOf(blue) with blue
        |------------
        |a
        |+ red = c -> false
        |== a
    |== *codeOf(blue) = a with codeOf(blue),a
    |------------
    |*codeOf(blue) = b with codeOf(blue),b
    |+ red = c -> false
        |------------
        |codeOf(blue) with blue
        |+ red = c -> false
            |------------
            |blue
            |+ red = c -> false
            |== blue
        |== codeOf(blue) with blue
        |------------
        |b
        |+ red = c -> false
        |== b
    |== *codeOf(blue) = b with codeOf(blue),b
|== (codeOf(blue) = a) ∨ (codeOf(blue) = b) with codeOf(blue) = a,codeOf(blue) = b
|------------
|(c = red) ∨ (c = blue) with c = red,c = blue
|+ red = c -> false
    |------------
    |*c = red with c,red
    |+ red = c -> false
        |------------
        |c
        |+ red = c -> false
        |== c
        |------------
        |red
        |+ red = c -> false
        |== red
    |== *c = red with c,red
    |------------
    |*c = blue with c,blue
    |+ red = c -> false
        |------------
        |c
        |+ red = c -> false
        |== c
        |------------
        |blue
        |+ red = c -> false
        |== blue
    |== *c = blue with c,blue
|== (c = red) ∨ (c = blue) with c = red,c = blue
|------------
|*codeOf(red) = b with codeOf(red),b
|+ red = c -> false
    |------------
    |codeOf(red) with red just ((red = c) ∧ (codeOf(red) = a)) ∨ ((~(red = c)) ∧ (codeOf(red) = b))
    |+ red = c -> false
        |------------
        |red
        |+ red = c -> false
        |== red
 definition:
        |------------
        |((red = c) ∧ (codeOf(red) = a)) ∨ ((~(red = c)) ∧ (codeOf(red) = b)) with ((red = c) ∧ (codeOf(red) = a)),((~(red = c)) ∧ (codeOf(red) = b))
        |+ red = c -> false
            |------------
            |((red = c) ∧ (codeOf(red) = a)) with (red = c) ∧ (codeOf(red) = a)
            |+ red = c -> false
                |------------
                |(red = c) ∧ (codeOf(red) = a) with red = c,codeOf(red) = a
                |+ red = c -> false
                    |------------
                    |*red = c with red,c
                    |+ red = c -> false
                    |== *!false with red,c
                |== !false with red = c
            |== !false with (red = c)
            |------------
            |((~(red = c)) ∧ (codeOf(red) = b)) with (~(red = c)) ∧ (codeOf(red) = b)
            |+ red = c -> false
                |------------
                |(~(red = c)) ∧ (codeOf(red) = b) with ~(red = c),codeOf(red) = b
                |+ red = c -> false
                    |------------
                    |~(red = c) with red = c
                    |+ red = c -> false
                        |------------
                        |*red = c with red,c
                        |+ red = c -> false
                        |== *!false with red,c
                    |== !true with red = c
                    |------------
                    |*codeOf(red) = b with codeOf(red),b
                    |+ red = c -> false
                        |------------
                        |codeOf(red) with red
                        |+ red = c -> false
                            |------------
                            |red
                            |+ red = c -> false
                            |== red
                        |== codeOf(red) with red
                        |------------
                        |b
                        |+ red = c -> false
                        |== b
                    |== *codeOf(red) = b with codeOf(red),b
                |== (codeOf(red) = b) with codeOf(red) = b
            |== ((codeOf(red) = b)) with (codeOf(red) = b)
        |== ((codeOf(red) = b)) with ((codeOf(red) = b))
    |== codeOf(red) with red just ((codeOf(red) = b))
    |------------
    |b
    |+ red = c -> false
    |== b
|== *codeOf(red) = b with codeOf(red),b
|------------
|*codeOf(red) = a with codeOf(red),a
|+ red = c -> false
|== *!false with codeOf(red),a
|------------
|*codeOf(blue) = a with codeOf(blue),a
|+ red = c -> false
    |------------
    |codeOf(blue) with blue
    |+ red = c -> false
        |------------
        |blue
        |+ red = c -> false
        |== blue
    |== codeOf(blue) with blue
    |------------
    |a
    |+ red = c -> false
    |== a
|== *codeOf(blue) = a with codeOf(blue),a
|------------
|*codeOf(blue) = b with codeOf(blue),b
|+ red = c -> false
    |------------
    |codeOf(blue) with blue
    |+ red = c -> false
        |------------
        |blue
        |+ red = c -> false
        |== blue
    |== codeOf(blue) with blue
    |------------
    |b
    |+ red = c -> false
    |== b
|== *codeOf(blue) = b with codeOf(blue),b
|------------
|*c = red with c,red
|+ red = c -> false
    |------------
    |c
    |+ red = c -> false
    |== c
    |------------
    |red
    |+ red = c -> false
    |== red
|== *c = red with c,red
|------------
|*c = blue with c,blue
|+ red = c -> false
    |------------
    |c
    |+ red = c -> false
    |== c
    |------------
    |blue
    |+ red = c -> false
    |== blue
|== *c = blue with c,blue
|------------
|(codeOf(blue) = a) ∨ (codeOf(blue) = b) with codeOf(blue) = a,codeOf(blue) = b
|+ codeOf(red) = a -> false
    |------------
    |*codeOf(blue) = a with codeOf(blue),a
    |+ codeOf(red) = a -> false
        |------------
        |codeOf(blue) with blue
        |+ codeOf(red) = a -> false
            |------------
            |blue
            |+ codeOf(red) = a -> false
            |== blue
        |== codeOf(blue) with blue
        |------------
        |a
        |+ codeOf(red) = a -> false
        |== a
    |== *codeOf(blue) = a with codeOf(blue),a
    |------------
    |*codeOf(blue) = b with codeOf(blue),b
    |+ codeOf(red) = a -> false
        |------------
        |codeOf(blue) with blue
        |+ codeOf(red) = a -> false
            |------------
            |blue
            |+ codeOf(red) = a -> false
            |== blue
        |== codeOf(blue) with blue
        |------------
        |b
        |+ codeOf(red) = a -> false
        |== b
    |== *codeOf(blue) = b with codeOf(blue),b
|== (codeOf(blue) = a) ∨ (codeOf(blue) = b) with codeOf(blue) = a,codeOf(blue) = b
|------------
|(c = red) ∨ (c = blue) with c = red,c = blue
|+ codeOf(red) = a -> false
    |------------
    |*c = red with c,red
    |+ codeOf(red) = a -> false
        |------------
        |c
        |+ codeOf(red) = a -> false
        |== c
        |------------
        |red
        |+ codeOf(red) = a -> false
        |== red
    |== *c = red with c,red
    |------------
    |*c = blue with c,blue
    |+ codeOf(red) = a -> false
        |------------
        |c
        |+ codeOf(red) = a -> false
        |== c
        |------------
        |blue
        |+ codeOf(red) = a -> false
        |== blue
    |== *c = blue with c,blue
|== (c = red) ∨ (c = blue) with c = red,c = blue
|------------
|*codeOf(red) = b with codeOf(red),b
|+ codeOf(red) = a -> false
    |------------
    |codeOf(red) with red just ((codeOf(red) = b))
    |+ codeOf(red) = a -> false
        |------------
        |red
        |+ codeOf(red) = a -> false
        |== red
 definition:
        |------------
        |((codeOf(red) = b)) with ((codeOf(red) = b))
        |+ codeOf(red) = a -> false
            |------------
            |((codeOf(red) = b)) with (codeOf(red) = b)
            |+ codeOf(red) = a -> false
                |------------
                |(codeOf(red) = b) with codeOf(red) = b
                |+ codeOf(red) = a -> false
                    |------------
                    |*codeOf(red) = b with codeOf(red),b
                    |+ codeOf(red) = a -> false
                        |------------
                        |codeOf(red) with red
                        |+ codeOf(red) = a -> false
                            |------------
                            |red
                            |+ codeOf(red) = a -> false
                            |== red
                        |== codeOf(red) with red
                        |------------
                        |b
                        |+ codeOf(red) = a -> false
                        |== b
                    |== *codeOf(red) = b with codeOf(red),b
                |== (codeOf(red) = b) with codeOf(red) = b
            |== ((codeOf(red) = b)) with (codeOf(red) = b)
        |== ((codeOf(red) = b)) with ((codeOf(red) = b))
    |== codeOf(red) with red just ((codeOf(red) = b))
    |------------
    |b
    |+ codeOf(red) = a -> false
    |== b
|== *codeOf(red) = b with codeOf(red),b
|------------
|*red = c with red,c
|+ codeOf(red) = a -> false
    |------------
    |red
    |+ codeOf(red) = a -> false
    |== red
    |------------
    |c
    |+ codeOf(red) = a -> false
    |== c
|== *red = c with red,c
|------------
|*codeOf(blue) = a with codeOf(blue),a
|+ codeOf(red) = a -> false
    |------------
    |codeOf(blue) with blue
    |+ codeOf(red) = a -> false
        |------------
        |blue
        |+ codeOf(red) = a -> false
        |== blue
    |== codeOf(blue) with blue
    |------------
    |a
    |+ codeOf(red) = a -> false
    |== a
|== *codeOf(blue) = a with codeOf(blue),a
|------------
|*codeOf(blue) = b with codeOf(blue),b
|+ codeOf(red) = a -> false
    |------------
    |codeOf(blue) with blue
    |+ codeOf(red) = a -> false
        |------------
        |blue
        |+ codeOf(red) = a -> false
        |== blue
    |== codeOf(blue) with blue
    |------------
    |b
    |+ codeOf(red) = a -> false
    |== b
|== *codeOf(blue) = b with codeOf(blue),b
|------------
|*c = red with c,red
|+ codeOf(red) = a -> false
    |------------
    |c
    |+ codeOf(red) = a -> false
    |== c
    |------------
    |red
    |+ codeOf(red) = a -> false
    |== red
|== *c = red with c,red
|------------
|*c = blue with c,blue
|+ codeOf(red) = a -> false
    |------------
    |c
    |+ codeOf(red) = a -> false
    |== c
    |------------
    |blue
    |+ codeOf(red) = a -> false
    |== blue
|== *c = blue with c,blue
|------------
|(codeOf(blue) = a) ∨ (codeOf(blue) = b) with codeOf(blue) = a,codeOf(blue) = b
|+ codeOf(blue) -> a
    |------------
    |*codeOf(blue) = a with codeOf(blue),a
    |+ codeOf(blue) -> a
        |------------
        |codeOf(blue) with blue
        |+ codeOf(blue) -> a
        |== !a with blue
        |------------
        |a
        |+ codeOf(blue) -> a
        |== a
    |== *!true with codeOf(blue),a
|== !true with codeOf(blue) = a
|------------
|(c = red) ∨ (c = blue) with c = red,c = blue
|+ codeOf(blue) -> a
    |------------
    |*c = red with c,red
    |+ codeOf(blue) -> a
        |------------
        |c
        |+ codeOf(blue) -> a
        |== c
        |------------
        |red
        |+ codeOf(blue) -> a
        |== red
    |== *c = red with c,red
    |------------
    |*c = blue with c,blue
    |+ codeOf(blue) -> a
        |------------
        |c
        |+ codeOf(blue) -> a
        |== c
        |------------
        |blue
        |+ codeOf(blue) -> a
        |== blue
    |== *c = blue with c,blue
|== (c = red) ∨ (c = blue) with c = red,c = blue
|------------
|*codeOf(red) = b with codeOf(red),b
|+ codeOf(blue) -> a
    |------------
    |codeOf(red) with red just ((codeOf(red) = b))
    |+ codeOf(blue) -> a
        |------------
        |red
        |+ codeOf(blue) -> a
        |== red
 definition:
        |------------
        |((codeOf(red) = b)) with ((codeOf(red) = b))
        |+ codeOf(blue) -> a
            |------------
            |((codeOf(red) = b)) with (codeOf(red) = b)
            |+ codeOf(blue) -> a
                |------------
                |(codeOf(red) = b) with codeOf(red) = b
                |+ codeOf(blue) -> a
                    |------------
                    |*codeOf(red) = b with codeOf(red),b
                    |+ codeOf(blue) -> a
                        |------------
                        |codeOf(red) with red
                        |+ codeOf(blue) -> a
                            |------------
                            |red
                            |+ codeOf(blue) -> a
                            |== red
                        |== codeOf(red) with red
                        |------------
                        |b
                        |+ codeOf(blue) -> a
                        |== b
                    |== *codeOf(red) = b with codeOf(red),b
                |== (codeOf(red) = b) with codeOf(red) = b
            |== ((codeOf(red) = b)) with (codeOf(red) = b)
        |== ((codeOf(red) = b)) with ((codeOf(red) = b))
    |== codeOf(red) with red just ((codeOf(red) = b))
    |------------
    |b
    |+ codeOf(blue) -> a
    |== b
|== *codeOf(red) = b with codeOf(red),b
|------------
|*red = c with red,c
|+ codeOf(blue) -> a
    |------------
    |red
    |+ codeOf(blue) -> a
    |== red
    |------------
    |c
    |+ codeOf(blue) -> a
    |== c
|== *red = c with red,c
|------------
|*codeOf(red) = a with codeOf(red),a
|+ codeOf(blue) -> a
|== *!false with codeOf(red),a
|------------
|*codeOf(blue) = b with codeOf(blue),b
|+ codeOf(blue) -> a
    |------------
    |codeOf(blue) with blue
    |+ codeOf(blue) -> a
    |== !a with blue
    |------------
    |b
    |+ codeOf(blue) -> a
    |== b
|== *!false with codeOf(blue),b
|------------
|*c = red with c,red
|+ codeOf(blue) -> a
    |------------
    |c
    |+ codeOf(blue) -> a
    |== c
    |------------
    |red
    |+ codeOf(blue) -> a
    |== red
|== *c = red with c,red
|------------
|*c = blue with c,blue
|+ codeOf(blue) -> a
    |------------
    |c
    |+ codeOf(blue) -> a
    |== c
    |------------
    |blue
    |+ codeOf(blue) -> a
    |== blue
|== *c = blue with c,blue
|------------
|(c = red) ∨ (c = blue) with c = red,c = blue
|+ codeOf(blue) -> a
    |------------
    |*c = red with c,red
    |+ codeOf(blue) -> a
        |------------
        |c
        |+ codeOf(blue) -> a
        |== c
        |------------
        |red
        |+ codeOf(blue) -> a
        |== red
    |== *c = red with c,red
    |------------
    |*c = blue with c,blue
    |+ codeOf(blue) -> a
        |------------
        |c
        |+ codeOf(blue) -> a
        |== c
        |------------
        |blue
        |+ codeOf(blue) -> a
        |== blue
    |== *c = blue with c,blue
|== (c = red) ∨ (c = blue) with c = red,c = blue
|------------
|*codeOf(red) = b with codeOf(red),b
|+ codeOf(blue) -> a
    |------------
    |codeOf(red) with red just ((codeOf(red) = b))
    |+ codeOf(blue) -> a
        |------------
        |red
        |+ codeOf(blue) -> a
        |== red
 definition:
        |------------
        |((codeOf(red) = b)) with ((codeOf(red) = b))
        |+ codeOf(blue) -> a
            |------------
            |((codeOf(red) = b)) with (codeOf(red) = b)
            |+ codeOf(blue) -> a
                |------------
                |(codeOf(red) = b) with codeOf(red) = b
                |+ codeOf(blue) -> a
                    |------------
                    |*codeOf(red) = b with codeOf(red),b
                    |+ codeOf(blue) -> a
                        |------------
                        |codeOf(red) with red
                        |+ codeOf(blue) -> a
                            |------------
                            |red
                            |+ codeOf(blue) -> a
                            |== red
                        |== codeOf(red) with red
                        |------------
                        |b
                        |+ codeOf(blue) -> a
                        |== b
                    |== *codeOf(red) = b with codeOf(red),b
                |== (codeOf(red) = b) with codeOf(red) = b
            |== ((codeOf(red) = b)) with (codeOf(red) = b)
        |== ((codeOf(red) = b)) with ((codeOf(red) = b))
    |== codeOf(red) with red just ((codeOf(red) = b))
    |------------
    |b
    |+ codeOf(blue) -> a
    |== b
|== *codeOf(red) = b with codeOf(red),b
|------------
|*red = c with red,c
|+ codeOf(blue) -> a
    |------------
    |red
    |+ codeOf(blue) -> a
    |== red
    |------------
    |c
    |+ codeOf(blue) -> a
    |== c
|== *red = c with red,c
|------------
|*codeOf(red) = a with codeOf(red),a
|+ codeOf(blue) -> a
|== *!false with codeOf(red),a
|------------
|*codeOf(blue) = b with codeOf(blue),b
|+ codeOf(blue) -> a
|== *!false with codeOf(blue),b
|------------
|*c = red with c,red
|+ codeOf(blue) -> a
    |------------
    |c
    |+ codeOf(blue) -> a
    |== c
    |------------
    |red
    |+ codeOf(blue) -> a
    |== red
|== *c = red with c,red
|------------
|*c = blue with c,blue
|+ codeOf(blue) -> a
    |------------
    |c
    |+ codeOf(blue) -> a
    |== c
    |------------
    |blue
    |+ codeOf(blue) -> a
    |== blue
|== *c = blue with c,blue
|------------
|(c = red) ∨ (c = blue) with c = red,c = blue
|+ codeOf(blue) = b -> false
    |------------
    |*c = red with c,red
    |+ codeOf(blue) = b -> false
        |------------
        |c
        |+ codeOf(blue) = b -> false
        |== c
        |------------
        |red
        |+ codeOf(blue) = b -> false
        |== red
    |== *c = red with c,red
    |------------
    |*c = blue with c,blue
    |+ codeOf(blue) = b -> false
        |------------
        |c
        |+ codeOf(blue) = b -> false
        |== c
        |------------
        |blue
        |+ codeOf(blue) = b -> false
        |== blue
    |== *c = blue with c,blue
|== (c = red) ∨ (c = blue) with c = red,c = blue
|------------
|*codeOf(red) = b with codeOf(red),b
|+ codeOf(blue) = b -> false
    |------------
    |codeOf(red) with red just ((codeOf(red) = b))
    |+ codeOf(blue) = b -> false
        |------------
        |red
        |+ codeOf(blue) = b -> false
        |== red
 definition:
        |------------
        |((codeOf(red) = b)) with ((codeOf(red) = b))
        |+ codeOf(blue) = b -> false
            |------------
            |((codeOf(red) = b)) with (codeOf(red) = b)
            |+ codeOf(blue) = b -> false
                |------------
                |(codeOf(red) = b) with codeOf(red) = b
                |+ codeOf(blue) = b -> false
                    |------------
                    |*codeOf(red) = b with codeOf(red),b
                    |+ codeOf(blue) = b -> false
                        |------------
                        |codeOf(red) with red
                        |+ codeOf(blue) = b -> false
                            |------------
                            |red
                            |+ codeOf(blue) = b -> false
                            |== red
                        |== codeOf(red) with red
                        |------------
                        |b
                        |+ codeOf(blue) = b -> false
                        |== b
                    |== *codeOf(red) = b with codeOf(red),b
                |== (codeOf(red) = b) with codeOf(red) = b
            |== ((codeOf(red) = b)) with (codeOf(red) = b)
        |== ((codeOf(red) = b)) with ((codeOf(red) = b))
    |== codeOf(red) with red just ((codeOf(red) = b))
    |------------
    |b
    |+ codeOf(blue) = b -> false
    |== b
|== *codeOf(red) = b with codeOf(red),b
|------------
|*red = c with red,c
|+ codeOf(blue) = b -> false
    |------------
    |red
    |+ codeOf(blue) = b -> false
    |== red
    |------------
    |c
    |+ codeOf(blue) = b -> false
    |== c
|== *red = c with red,c
|------------
|*codeOf(blue) = a with codeOf(blue),a
|+ codeOf(blue) = b -> false
    |------------
    |codeOf(blue) with blue
    |+ codeOf(blue) = b -> false
        |------------
        |blue
        |+ codeOf(blue) = b -> false
        |== blue
    |== codeOf(blue) with blue
    |------------
    |a
    |+ codeOf(blue) = b -> false
    |== a
|== *codeOf(blue) = a with codeOf(blue),a
|------------
|*c = red with c,red
|+ codeOf(blue) = b -> false
    |------------
    |c
    |+ codeOf(blue) = b -> false
    |== c
    |------------
    |red
    |+ codeOf(blue) = b -> false
    |== red
|== *c = red with c,red
|------------
|*c = blue with c,blue
|+ codeOf(blue) = b -> false
    |------------
    |c
    |+ codeOf(blue) = b -> false
    |== c
    |------------
    |blue
    |+ codeOf(blue) = b -> false
    |== blue
|== *c = blue with c,blue
|------------
|(c = red) ∨ (c = blue) with c = red,c = blue
|+ c = red -> false
    |------------
    |*c = red with c,red
    |+ c = red -> false
    |== *!false with c,red
    |------------
    |*c = blue with c,blue
    |+ c = red -> false
        |------------
        |c
        |+ c = red -> false
        |== c
        |------------
        |blue
        |+ c = red -> false
        |== blue
    |== *c = blue with c,blue
|== (c = blue) with c = blue
|------------
|*codeOf(red) = b with codeOf(red),b
|+ c = red -> false
    |------------
    |codeOf(red) with red just ((codeOf(red) = b))
    |+ c = red -> false
        |------------
        |red
        |+ c = red -> false
        |== red
 definition:
        |------------
        |((codeOf(red) = b)) with ((codeOf(red) = b))
        |+ c = red -> false
            |------------
            |((codeOf(red) = b)) with (codeOf(red) = b)
            |+ c = red -> false
                |------------
                |(codeOf(red) = b) with codeOf(red) = b
                |+ c = red -> false
                    |------------
                    |*codeOf(red) = b with codeOf(red),b
                    |+ c = red -> false
                        |------------
                        |codeOf(red) with red
                        |+ c = red -> false
                            |------------
                            |red
                            |+ c = red -> false
                            |== red
                        |== codeOf(red) with red
                        |------------
                        |b
                        |+ c = red -> false
                        |== b
                    |== *codeOf(red) = b with codeOf(red),b
                |== (codeOf(red) = b) with codeOf(red) = b
            |== ((codeOf(red) = b)) with (codeOf(red) = b)
        |== ((codeOf(red) = b)) with ((codeOf(red) = b))
    |== codeOf(red) with red just ((codeOf(red) = b))
    |------------
    |b
    |+ c = red -> false
    |== b
|== *codeOf(red) = b with codeOf(red),b
|------------
|*red = c with red,c
|+ c = red -> false
    |------------
    |red
    |+ c = red -> false
    |== red
    |------------
    |c
    |+ c = red -> false
    |== c
|== *red = c with red,c
|------------
|*codeOf(red) = a with codeOf(red),a
|+ c = red -> false
|== *!false with codeOf(red),a
|------------
|*codeOf(blue) = a with codeOf(blue),a
|+ c = red -> false
    |------------
    |codeOf(blue) with blue
    |+ c = red -> false
        |------------
        |blue
        |+ c = red -> false
        |== blue
    |== codeOf(blue) with blue
    |------------
    |a
    |+ c = red -> false
    |== a
|== *codeOf(blue) = a with codeOf(blue),a
|------------
|*codeOf(blue) = b with codeOf(blue),b
|+ c = red -> false
|== *!false with codeOf(blue),b
|------------
|*c = blue with c,blue
|+ c = red -> false
    |------------
    |c
    |+ c = red -> false
    |== c
    |------------
    |blue
    |+ c = red -> false
    |== blue
|== *c = blue with c,blue
|------------
|*codeOf(red) = b with codeOf(red),b
|+ c -> blue
    |------------
    |codeOf(red) with red just ((codeOf(red) = b))
    |+ c -> blue
        |------------
        |red
        |+ c -> blue
        |== red
 definition:
        |------------
        |((codeOf(red) = b)) with ((codeOf(red) = b))
        |+ c -> blue
            |------------
            |((codeOf(red) = b)) with (codeOf(red) = b)
            |+ c -> blue
                |------------
                |(codeOf(red) = b) with codeOf(red) = b
                |+ c -> blue
                    |------------
                    |*codeOf(red) = b with codeOf(red),b
                    |+ c -> blue
                        |------------
                        |codeOf(red) with red
                        |+ c -> blue
                            |------------
                            |red
                            |+ c -> blue
                            |== red
                        |== codeOf(red) with red
                        |------------
                        |b
                        |+ c -> blue
                        |== b
                    |== *codeOf(red) = b with codeOf(red),b
                |== (codeOf(red) = b) with codeOf(red) = b
            |== ((codeOf(red) = b)) with (codeOf(red) = b)
        |== ((codeOf(red) = b)) with ((codeOf(red) = b))
    |== codeOf(red) with red just ((codeOf(red) = b))
    |------------
    |b
    |+ c -> blue
    |== b
|== *codeOf(red) = b with codeOf(red),b
|------------
|*red = c with red,c
|+ c -> blue
    |------------
    |red
    |+ c -> blue
    |== red
    |------------
    |c
    |+ c -> blue
    |== !blue
|== *!false with red,c
|------------
|*codeOf(red) = a with codeOf(red),a
|+ c -> blue
|== *!false with codeOf(red),a
|------------
|*codeOf(blue) = a with codeOf(blue),a
|+ c -> blue
    |------------
    |codeOf(blue) with blue
    |+ c -> blue
        |------------
        |blue
        |+ c -> blue
        |== blue
    |== codeOf(blue) with blue
    |------------
    |a
    |+ c -> blue
    |== a
|== *codeOf(blue) = a with codeOf(blue),a
|------------
|*codeOf(blue) = b with codeOf(blue),b
|+ c -> blue
|== *!false with codeOf(blue),b
|------------
|*c = red with c,red
|+ c -> blue
    |------------
    |c
    |+ c -> blue
    |== !blue
    |------------
    |red
    |+ c -> blue
    |== red
|== *!false with c,red
|------------
|*codeOf(red) = b with codeOf(red),b
|+ c -> blue
    |------------
    |codeOf(red) with red just ((codeOf(red) = b))
    |+ c -> blue
        |------------
        |red
        |+ c -> blue
        |== red
 definition:
        |------------
        |((codeOf(red) = b)) with ((codeOf(red) = b))
        |+ c -> blue
            |------------
            |((codeOf(red) = b)) with (codeOf(red) = b)
            |+ c -> blue
                |------------
                |(codeOf(red) = b) with codeOf(red) = b
                |+ c -> blue
                    |------------
                    |*codeOf(red) = b with codeOf(red),b
                    |+ c -> blue
                        |------------
                        |codeOf(red) with red
                        |+ c -> blue
                            |------------
                            |red
                            |+ c -> blue
                            |== red
                        |== codeOf(red) with red
                        |------------
                        |b
                        |+ c -> blue
                        |== b
                    |== *codeOf(red) = b with codeOf(red),b
                |== (codeOf(red) = b) with codeOf(red) = b
            |== ((codeOf(red) = b)) with (codeOf(red) = b)
        |== ((codeOf(red) = b)) with ((codeOf(red) = b))
    |== codeOf(red) with red just ((codeOf(red) = b))
    |------------
    |b
    |+ c -> blue
    |== b
|== *codeOf(red) = b with codeOf(red),b
|------------
|*red = c with red,c
|+ c -> blue
|== *!false with red,c
|------------
|*codeOf(red) = a with codeOf(red),a
|+ c -> blue
|== *!false with codeOf(red),a
|------------
|*codeOf(blue) = a with codeOf(blue),a
|+ c -> blue
    |------------
    |codeOf(blue) with blue
    |+ c -> blue
        |------------
        |blue
        |+ c -> blue
        |== blue
    |== codeOf(blue) with blue
    |------------
    |a
    |+ c -> blue
    |== a
|== *codeOf(blue) = a with codeOf(blue),a
|------------
|*codeOf(blue) = b with codeOf(blue),b
|+ c -> blue
|== *!false with codeOf(blue),b
|------------
|*c = red with c,red
|+ c -> blue
|== *!false with c,red