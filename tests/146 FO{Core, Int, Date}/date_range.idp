vocabulary {
    type dates
    date: () -> dates
}

theory {
    date() < date()+1.
    #TODAY(0,0,-1) < #TODAY.
}

structure {
    dates := {#2021-01-01..#2021-01-10}.
}
procedure main() {
    print(Theory(T,S).formula())
}