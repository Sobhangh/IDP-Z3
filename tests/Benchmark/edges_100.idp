vocabulary V {
type Node
Edge: Node * Node -> Bool
}

structure S  {
Node := {1..100}.
}

theory T:V  {
!x,y: x ~= y => (Edge(x,y) | Edge(y,x)).
!x,y: x ~= y => (~Edge(x,y) | ~Edge(y,x)).
!x: ?y: x ~= y & Edge(x,y).
}

procedure main() {
    pretty_print(model_expand(T, S, max=1))
}
