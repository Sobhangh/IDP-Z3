vocabulary V {
    type color := {a, b}
    col : color -> Bool
    p: color -> Bool
}

theory T:V {
    col := {a}.
    ?x in col: p(x).
}
procedure main() {
    Th = Theory(T)
    print("Formula: ")
    pretty_print(Th.formula())
}