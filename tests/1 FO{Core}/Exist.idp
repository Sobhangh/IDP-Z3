vocabulary {
    type color := {red, blue, green}
    p: () → Bool
    q: (color) → Bool
    var c in color
}
theory {
    p() <=> ?c in color: c~=red.
    ?c: q(c).
}
display {
    view() = expanded.
}