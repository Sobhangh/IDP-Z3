vocabulary V {
    type byte := {1,2}
    type color
    Color: () -> color
    opposite: color → color
}

theory T:V {
    opposite(Color()) is enumerated.
}

structure S:V {
    color := constructed from {red, blue, green, rgb(R: byte, byte)}
    opposite := {red-> green, green-> red}
}

procedure main() {
  problem = Problem(T, S)
  pretty_print(problem.formula())
  pretty_print(problem.expand(max=1))
}