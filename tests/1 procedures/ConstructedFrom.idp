vocabulary V {
    type byte := {1,2}
    type color
    Color: () -> color
    opposite: color → color
}

theory T:V {
    opposite(Color()) is enumerated.
}

structure S:V {
    color := constructed from {red, blue, green, rgb(R: byte, byte)}
    opposite := {red-> green, green-> red}
}

procedure main() {
  pretty_print(Problem(T, S).formula())
  pretty_print(Problem(T,S).expand(max=1))
}