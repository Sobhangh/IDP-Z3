vocabulary V {
    type byte := {t,u}
    type color := constructed from {red, blue, green, rgb(R: byte, byte)}
    Color: () -> color
}

theory T:V {
    R(rgb(t,u)) = t.
}
display {
    goal(`Color).
}
