vocabulary {
    type color := {red, blue, green}
    Color: () -> color
    p: ()→Bool
}

theory {
    p() => Color() in {red, blue}.
}

procedure main() {
    print(model_propagate(T, sort=True))
    print(Problem(T).formula())
    print(model_expand(T, sort=True))
}
