vocabulary {
    type color constructed from {red, blue, green}
    Color: () -> color
    p: ()
}

theory {
    p => Color() in {red; blue}.
}

procedure main() {
    print(model_propagate(T))
    print(Problem(T).formula())
    print(model_expand(T))
}
