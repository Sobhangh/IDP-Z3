vocabulary V {
    type Color := {red, blue, green}
    p
    q(Color)
    color(): Color
    r
}
theory T:V {
    p <=> (∀c[Color]: c=red).
    q := {red, blue}
    (∀c[Color]: ~q(c) => c=color()).
}
procedure main() {
    print(str(Problem(T).formula()))
}