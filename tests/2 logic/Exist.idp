vocabulary {
    type color constructed from {red, blue, green}
    p: () → Bool
    q: (color) → Bool
}
theory {
    p() <=> (?c[color]: c~=red).
    ?c[color]: q(c).
}
display {
    view() = expanded.
}